const btn = document.querySelector('.j-btn-test');

btn.addEventListener('click', () => {
  alert(`размер экрана с учетом полосы прокрутки: ${window.innerWidth} x ${window.innerHeight}`);
  });






